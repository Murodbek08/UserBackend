generator client {
  provider = "prisma-client-js"
  output   = "./generated"  // Shu papka yaratiladi
}

datasource db {
  provider = "postgresql"
}

model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  name      String
}

